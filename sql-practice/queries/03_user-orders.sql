-- Задача: все заказы пользователя с id = 1
-- Зачем в тестировании: после оформления заказа проверяем, что он привязался
-- к нужному юзеру, а не улетел к соседу. Классическое "а давай глянем в базе".

SELECT id, user_id, total, status, created_at
FROM orders
WHERE user_id = 1;
