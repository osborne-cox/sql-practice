-- Задача: показать всех активных пользователей
-- Зачем в тестировании: проверяем, что флаг is_active реально что-то фильтрует,
-- а не просто висит в таблице для красоты. Петр (is_active = FALSE) попасть сюда не должен.

SELECT id, name, email
FROM users
WHERE is_active = TRUE;
