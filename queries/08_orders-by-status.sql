-- Задача: количество заказов по статусам
-- Зачем в тестировании: проверяем, что статусы не "протекают" и среди них нет
-- неожиданных значений (опечатки, пустые, старые статусы из прошлых релизов).

SELECT status, COUNT(*) AS orders_count
FROM orders
GROUP BY status
ORDER BY orders_count DESC;
