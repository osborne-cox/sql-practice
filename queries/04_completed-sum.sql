-- Задача: сумма всех завершённых (completed) заказов
-- Зачем в тестировании: сверка выручки. Цифра из отчёта в интерфейсе должна
-- совпадать с тем, что реально лежит в базе. Не совпало — копаем дальше.

SELECT SUM(total) AS completed_total
FROM orders
WHERE status = 'completed';
